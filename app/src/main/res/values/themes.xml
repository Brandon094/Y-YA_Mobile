<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema Principal de la App (Jetpack Compose) -->
    <style name="Theme.Yáya2" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema para el Splash Screen (Soporte Modo Claro/Oscuro) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Yáya2</item>
    </style>
</resources>