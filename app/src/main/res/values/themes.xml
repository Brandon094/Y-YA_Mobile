<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Yáya2" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Fondo de marca Premium -->
        <item name="windowSplashScreenBackground">#E85C5C</item>

        <!-- Icono adaptativo (Android se encarga del recorte circular perfecto) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>

        <item name="postSplashScreenTheme">@style/Theme.Yáya2</item>
    </style>
</resources>
